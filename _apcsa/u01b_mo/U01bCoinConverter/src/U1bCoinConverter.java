class U01bCoinConverter {
  public static String convertCoins(double amount) {
    // TODO write code to determine how many dollars, quarters, dimes, nickels, and pennies
    // If the ammount is 2.44, then it should return "2 dollars, 1 quarters, 1 dimes, 1 nickels, 4 pennies"
    // Your return statement should be `return dollars + "dollars, " + quarters + " quarters, " + dimes + " dimes, " + nickels + " nickels, " + pennies + " pennies";`
    return "";
  }
  public static void main(String[] args) {
    System.out.print("Enter the amount to convert to coins in dollars and cents (for example, 3.41): ");
    
  }
}